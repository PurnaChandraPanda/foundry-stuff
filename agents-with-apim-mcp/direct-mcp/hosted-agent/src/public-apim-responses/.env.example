# Tenant of the subscription containing the Foundry resource.
AZURE_TENANT_ID="16---------------------------------d3"

# Existing project ARM resource ID for azd initialization, not read by the agent.
AZURE_AI_PROJECT_ID="/subscriptions/<subscription-id>/resourceGroups/<resource-group>/providers/Microsoft.CognitiveServices/accounts/<account>/projects/<project>"
AZURE_AI_PROJECT_ENDPOINT="https://<account>.services.ai.azure.com/api/projects/<project>"
AZURE_AI_MODEL_DEPLOYMENT_NAME="<model-deployment-name>"

# Use the exact Streamable HTTP MCP gateway URL, not the APIM management URL.
APIM_MCP_URL="https://<public-apim-host>/<api-suffix>/mcp"
APIM_MCP_SERVER_LABEL="public-apim"

# RemoteTool connection
APIM_MCP_CONNECTION_NAME="public-apim-mcp"

# Discover names with diagnose_mcp.py; explicitly select permitted tools.
APIM_ALLOWED_TOOLS=""
MCP_TIMEOUT_SECONDS="120"
APIM_ALLOW_UNATTENDED="true"

# Change only after reviewing the allowlist and effective backend permissions.
MCP_TIMEOUT_SECONDS="120"
PORT="8088"
